/**
 * {{ project_name_human }} — Main stylesheet
 * Generated: {{ date }}
 * Author:    {{ author }}
 */

/* ─── Base ─────────────────────────────────────────────── */
.{{ project_name | replace("_", "-") }}-archive,
.{{ project_name | replace("_", "-") }}-single {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1rem;
}