"""
Tests for {{ project_name_human }}.

Add your test cases below. Run with:
    python -m pytest tests/
"""

from {{ project_name }}.main import main


def test_main_runs() -> None:
    """Verify that main() executes without raising an exception."""
    # Smoke test — replace with meaningful assertions as you build.
    main()
